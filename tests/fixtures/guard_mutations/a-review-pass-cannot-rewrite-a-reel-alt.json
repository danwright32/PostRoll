{
  "name": "a-review-pass-cannot-rewrite-a-reel-alt",
  "file": "postroll/ai/generate_captions.py",
  "find": "        data = dict(data, alt_texts=draft_alt_texts)",
  "replace": "        pass",
  "test": "tests/test_reel_alt_text_describes_the_reel.py::test_a_reel_alt_condensed_under_its_own_floor_is_still_refused",
  "breaks": "a refused review pass keeps its own alt text anyway, so the refusal is a message with no effect: a reel level description condensed into one frame's still ships, and a per photo post's reordered alt texts still sit beside the wrong photographs (#1067, #1214)"
}
