{
  "name": "a-switch-rebuilds-every-day-again",
  "file": "PostRollApp/Sources/Views/PostingLayoutControl.swift",
  "find": "        let work = PostingLayoutSwitch.work(\n            PostingLayoutSwitch.plan(from: old, to: newValue, in: ev))",
  "replace": "        let work = PostingLayoutSwitch.Work(\n            rebuildDays: Set(newValue.affectedDays(in: ev).map(\\.rawValue)), redrawDays: [])",
  "test": "PostRollTests/PostingLayoutCopyTests/testTheControlAsksThePlanWhatToRebuild",
  "breaks": "a layout switch goes back to sending every governed day with photos to the caption generator, so Balanced to Opening pays for Monday and Wednesday when neither changes and replaces any caption Dan typed on them (#1010)"
}
