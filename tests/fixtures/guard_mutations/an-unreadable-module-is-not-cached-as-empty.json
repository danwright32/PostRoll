{
  "name": "an-unreadable-module-is-not-cached-as-empty",
  "file": "tools/check_guards.py",
  "find": "        # one fault two names and the wrong one first (L11).\n        return frozenset()",
  "replace": "        # one fault two names and the wrong one first (L11).\n        _SILENCED_CACHE[key] = frozenset()\n        return _SILENCED_CACHE[key]",
  "test": "tests/test_check_guards.py::test_a_module_that_could_not_be_read_is_not_remembered_as_silencing_nobody",
  "breaks": "a test module that could not be parsed is remembered as one that silences nobody, so once the two share a representation nothing can tell a non-answer from a measurement of nothing, for the rest of the process (#931, L215)"
}
