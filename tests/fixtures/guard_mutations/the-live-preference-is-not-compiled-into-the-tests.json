{
  "name": "the-live-preference-is-not-compiled-into-the-tests",
  "file": "PostRollApp/Sources/Services/PostingPresetStore.swift",
  "find": "    init(defaults: UserDefaults) {",
  "replace": "    init(defaults: UserDefaults = .standard) {",
  "test": "tests/test_swift_tests_never_reach_live_data.py::test_no_store_the_test_bundle_compiles_defaults_to_a_live_location",
  "breaks": "the initializer the test bundle compiles defaults to Dan's real posting preference again, so a test rendering the settings screen reads his layout and a rendered control would write it back. Its own entry rather than leaning on the analytics one: that perturbation only ever exercises the AppPaths half of the rule, and the preferences half is the half this guard could not see until #727 (L151, L178)"
}
