{
  "name": "a-rewritten-reel-alt-is-reported",
  "file": "postroll/ai/generate_captions.py",
  "find": "            detail=(f\"The pass {shape_fault}. \"",
  "replace": "            detail=(f\"\"",
  "test": "tests/test_alt_text_photo_anchor.py::test_the_reorder_report_says_it_was_a_reorder",
  "breaks": "a shape fault reaches the review screen naming nothing, so a reorder, a dropped entry and an added one all read the same. Putting the draft's alt text back destroys the only evidence a pass ignored the instruction, and a report that cannot say WHICH fault happened leaves the reader unable to act (L11, L340)"
}
