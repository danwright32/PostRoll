{
  "name": "an-absent-secret-is-not-exported-as-blank",
  "file": "PostRollApp/Sources/Services/PythonBridge.swift",
  "find": "            guard let value = secret.value,\n                  !value.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty\n            else { continue }",
  "replace": "            let value = secret.value ?? \"\"",
  "test": "PostRollTests/APIKeyDeliveryTests/testOneSecretMissingDoesNotStopTheOtherBeingDelivered",
  "breaks": "a secret nobody has set is exported as an empty string, so the reader sees a value that was rejected rather than one that was never provided, and reports the wrong remedy (#1002, L138)"
}
