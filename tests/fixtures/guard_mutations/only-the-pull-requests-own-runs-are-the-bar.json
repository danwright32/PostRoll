{
  "name": "only-the-pull-requests-own-runs-are-the-bar",
  "file": "tools/wait_for_checks.py",
  "find": "        if run.get(\"event\") != \"pull_request\":",
  "replace": "        if False:",
  "test": "tests/test_wait_for_checks.py::test_a_run_triggered_by_something_other_than_the_pull_request_is_not_the_bar",
  "breaks": "a workflow that also runs on push reports its jobs twice at one commit, under names the bar holds once, so which of the two runs answers for a check is decided by the order GitHub happened to list them in"
}
