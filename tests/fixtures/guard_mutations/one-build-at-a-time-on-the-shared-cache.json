{
  "name": "one-build-at-a-time-on-the-shared-cache",
  "file": "tools/check_guards.py",
  "find": "            fcntl.flock(handle, fcntl.LOCK_EX | fcntl.LOCK_NB)",
  "replace": "            pass",
  "test": "tests/test_check_guards.py::test_two_runs_do_not_build_at_the_same_time",
  "breaks": "two builds write into one DerivedData at once, which produces errors that read like real compile failures in whichever run notices first, and the sweep would report that as a guard ERROR rather than as contention (#642)"
}
