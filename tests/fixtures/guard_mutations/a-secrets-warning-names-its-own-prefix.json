{
  "name": "a-secrets-warning-names-its-own-prefix",
  "file": "PostRollApp/Sources/Services/KeychainStore.swift",
  "find": "        guard cleaned.hasPrefix(secret.prefix) else {",
  "replace": "        guard cleaned.hasPrefix(Secret.anthropic.prefix) else {",
  "test": "PostRollTests/KeychainStoreTests/testAGoodMetaTokenIsNotToldItShouldStartWithTheAnthropicPrefix",
  "breaks": "the warning beside every secret field checks one hard wired prefix, so a perfectly good Meta token is labelled as not starting with sk-ant- while Save stays live, which is the defect the descriptor exists to remove (#1002)"
}
