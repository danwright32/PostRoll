{
  "name": "one-findings-panel",
  "file": "PostRollApp/Sources/Views/CaptionReviewView.swift",
  "find": "            FindingsPanel(summary: summary,\n                          findings: caption.findings,\n                          isStale: caption.findingsAreStale,\n                          subject: \"caption\")",
  "replace": "            ForEach(FindingsDisplay.grouped(findings: caption.findings), id: \\.code) { group in\n                Text(group.message)\n            }",
  "test": "PostRollTests/FindingsPanelTests/testOnlyOneViewGroupsTheFindings",
  "breaks": "a second copy of the findings panel grows back in a screen file, which is how the two of them drifted into reading as one labelled group on one screen and loose fragments on the other (#603)"
}
