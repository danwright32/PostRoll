{
  "name": "each-secret-reaches-the-subprocess-under-its-own-carrier",
  "file": "PostRollApp/Sources/Services/PythonBridge.swift",
  "find": "            let carrier = \"POSTROLL_\\(secret.variable)\"",
  "replace": "            let carrier = \"POSTROLL_SECRET\"",
  "test": "PostRollTests/APIKeyDeliveryTests/testEachSecretCarriesItsOwnCarrierName",
  "breaks": "two secrets share one environment carrier, so the second overwrites the first on the way to the Python subprocess and whichever feature runs second fails to authenticate with no sign of why (#1002)"
}
