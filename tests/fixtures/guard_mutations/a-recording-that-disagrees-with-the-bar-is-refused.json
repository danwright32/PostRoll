{
  "name": "a-recording-that-disagrees-with-the-bar-is-refused",
  "file": "tools/record_check_fixtures.py",
  "find": "    if reported != wanted:",
  "replace": "    if False:",
  "test": "tests/test_wait_for_checks.py::test_a_reply_that_does_not_match_the_derived_bar_is_not_recorded",
  "breaks": "a recording is kept even when the checks it reports are not the bar derived at that commit, so the calibration and the thing it calibrates drift together and nothing is left that says what GitHub actually replies (#1343, L58)"
}
