{
  "name": "failed-downloads-are-not-no-matches",
  "file": "postroll/audio.py",
  "find": "    if kept == 0:\n        raise JamendoUnavailable(",
  "replace": "    if False:\n        raise JamendoUnavailable(",
  "test": "tests/test_audio_download_failures.py::test_every_download_failing_is_not_reported_as_nothing_matching",
  "breaks": "every candidate download failing returns an empty list, indistinguishable from a search that matched nothing, and only one of those is retryable (#453)"
}
