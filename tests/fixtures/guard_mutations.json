{
  "comment": "One recorded perturbation per named guard (#416). `venv/bin/python tools/check_guards.py` (or `make check-guards`) applies each entry's find/replace to the file the guard protects, runs only that test, requires it to FAIL, and restores the file. Run it whenever a guard is added or changed. Perturbations must keep the code compiling: a build error fails every test trivially and proves nothing, so the checker reports it as an error rather than a kill. `tests/test_guard_mutation_registry.py` holds every anchor and test name to the code on every suite run.",
  "entries": [
    {
      "name": "archive-note-wraps",
      "file": "PostRollApp/Sources/Views/ExportDoneSummary.swift",
      "find": ".fixedSize(horizontal: false, vertical: true)",
      "replace": ".fixedSize(horizontal: false, vertical: false)",
      "test": "PostRollTests/HostedControlLegibilityTests/testTheArchiveNoteStillDeclaresThatItWraps",
      "breaks": "the archive note goes back to one line and drops the end of its sentence (#411)"
    },
    {
      "name": "new-event-refusal-drawn",
      "file": "PostRollApp/Sources/Views/NewEventSheet.swift",
      "find": "RefusalNote(message: refusal)",
      "replace": "EmptyView()",
      "test": "PostRollTests/VisibleRefusalGuardTests/testTheNewEventSheetDrawsWhatIsMissing",
      "breaks": "the sheet computes which field is empty and never draws it (#402)"
    },
    {
      "name": "stage-strip-refusal-drawn",
      "file": "PostRollApp/Sources/Views/ProgramUploadView.swift",
      "find": "RefusalNote(message: refusal)",
      "replace": "EmptyView()",
      "test": "PostRollTests/VisibleRefusalGuardTests/testTheStageStripDrawsItsRefusalRatherThanHidingItOnHover",
      "breaks": "the stage strip refusal is computed and never drawn (#402)"
    },
    {
      "name": "new-event-refusal-not-satisfied-by-a-comment",
      "file": "PostRollApp/Sources/Views/NewEventSheet.swift",
      "find": "RefusalNote(message: refusal)",
      "replace": "EmptyView() // RefusalNote(message: refusal)",
      "test": "PostRollTests/VisibleRefusalGuardTests/testTheNewEventSheetDrawsWhatIsMissing",
      "breaks": "the refusal is gone and a trailing comment still names it (L103)"
    },
    {
      "name": "archive-note-not-satisfied-by-a-comment",
      "file": "PostRollApp/Sources/Views/ExportDoneSummary.swift",
      "find": ".fixedSize(horizontal: false, vertical: true)",
      "replace": ".opacity(1) // .fixedSize(horizontal: false, vertical: true)",
      "test": "PostRollTests/HostedControlLegibilityTests/testTheArchiveNoteStillDeclaresThatItWraps",
      "breaks": "the wrap declaration is gone and a trailing comment still names it (L103)"
    },
    {
      "name": "refusal-note-ink",
      "file": "PostRollApp/Sources/Views/SharedChrome.swift",
      "find": ".fixedSize(horizontal: false, vertical: true)\n        }\n        .foregroundStyle(Color.warmMid)",
      "replace": ".fixedSize(horizontal: false, vertical: true)\n        }\n        .foregroundStyle(Color.cream)",
      "test": "PostRollTests/BannerLegibilityTests/testEveryBannerActuallyDrawsItsMessage",
      "breaks": "RefusalNote draws cream on cream, the invisible-type defect that has shipped three times (#389)"
    },
    {
      "name": "fast-subset-deselects",
      "file": "Makefile",
      "find": "-m \"not slow\"",
      "replace": "-m \"not fast\"",
      "test": "tests/test_fast_subset_stays_honest.py::test_there_is_a_fast_target_and_it_is_not_the_gate",
      "breaks": "the fast target quietly stops deselecting anything"
    },
    {
      "name": "reference-frames-parallel",
      "file": ".github/workflows/swift.yml",
      "find": "-n auto -v -ra",
      "replace": "-v -ra",
      "test": "tests/test_reference_frames_are_safe_to_parallelise.py::test_the_step_still_runs_in_parallel",
      "breaks": "the reference frame step silently goes back to serial"
    },
    {
      "name": "ci-unit-scheme",
      "file": ".github/workflows/swift.yml",
      "find": "-scheme PostRollTests",
      "replace": "-scheme PostRoll",
      "test": "tests/test_ci_gates.py::test_it_runs_the_unit_test_scheme_not_the_gui_one",
      "breaks": "CI runs the GUI-driving scheme a headless runner cannot drive"
    },
    {
      "name": "install-script-portability",
      "file": "PostRollApp/build-install.sh",
      "find": "SWIFT_LOG=\"$(mktemp \"${TMPDIR:-/tmp}/postroll-swift-tests.XXXXXX\")\"",
      "replace": "SWIFT_LOG=\"$(mktemp -t postroll-swift-tests)\"",
      "test": "tests/test_build_and_test_gates.py::test_the_install_script_uses_no_bsd_only_shell_forms",
      "breaks": "a BSD-only shell form returns to the install script (CI is Linux)"
    },
    {
      "name": "prepush-icloud-checker",
      "file": ".githooks/pre-push",
      "find": "check_icloud_duplicates.py",
      "replace": "check_icloud_dupes.py",
      "test": "tests/test_icloud_duplicates.py::test_the_pre_push_hook_runs_the_checker",
      "breaks": "the pre-push hook stops running the iCloud duplicate checker"
    },
    {
      "name": "collage-design-version-parity",
      "file": "PostRollApp/Sources/DesignTokens.swift",
      "find": "static let collageDesignVersion = 1",
      "replace": "static let collageDesignVersion = 2",
      "test": "tests/test_collage_design_version.py::test_swift_mirrors_the_same_version",
      "breaks": "the Swift collage design version drifts from the Python one"
    },
    {
      "name": "design-stamp-filename-parity",
      "file": "PostRollApp/Sources/Services/DesignStamp.swift",
      "find": "static let stampName = \"design.json\"",
      "replace": "static let stampName = \"designs.json\"",
      "test": "tests/test_media_design_version.py::test_swift_uses_the_same_stamp_filename",
      "breaks": "Swift reads a different stamp file than Python writes"
    },
    {
      "name": "palette-mirror-parity",
      "file": "PostRollApp/Sources/DesignTokens.swift",
      "find": "static let hairline  = Color(red: 214/255, green: 208/255, blue: 200/255)",
      "replace": "static let hairline  = Color(red: 212/255, green: 201/255, blue: 192/255)",
      "test": "tests/test_design_tokens.py::test_the_swift_mirror_agrees_with_the_python_tokens[hairline-HAIRLINE]",
      "breaks": "the Swift hairline colour drifts from the Python palette"
    },
    {
      "name": "crop-anchor-swift-parity",
      "file": "PostRollApp/Sources/Models/Event.swift",
      "find": "static let topAnchoredY: Double = -1.0",
      "replace": "static let topAnchoredY: Double = -0.5",
      "test": "tests/test_top_anchored_crop.py::test_swift_and_python_agree_on_the_anchor",
      "breaks": "the Swift crop anchor drifts from Python and crops start eating the top (#167)"
    },
    {
      "name": "crop-anchor-python-parity",
      "file": "postroll/media/generate_collage.py",
      "find": "TOP_ANCHORED_CROP_Y = -1.0",
      "replace": "TOP_ANCHORED_CROP_Y = -0.9",
      "test": "tests/test_crop_geometry_parity.py::test_the_fixture_constants_match_the_implementation",
      "breaks": "the Python crop anchor drifts from the shared crop fixture (#168)"
    },
    {
      "name": "vision-thinness-parity",
      "file": "PostRollApp/Sources/Services/VisionTextLayer.swift",
      "find": "static let minimumWords = 8",
      "replace": "static let minimumWords = 9",
      "test": "tests/test_vision_cross_check.py::test_the_swift_mirror_of_the_thinness_rule_agrees",
      "breaks": "the Swift thinness rule drifts from the Python one"
    },
    {
      "name": "subscription-env-parity",
      "file": "PostRollApp/Sources/Services/HaltedWeek.swift",
      "find": "static let subscriptionEnv = \"POSTROLL_USE_SUBSCRIPTION\"",
      "replace": "static let subscriptionEnv = \"POSTROLL_USE_SUBSCRIPTIONS\"",
      "test": "tests/test_subscription_transport.py::test_the_swift_mirror_of_the_switch_name_agrees",
      "breaks": "Swift flips a different switch than Python reads"
    },
    {
      "name": "shoot-type-parity",
      "file": "PostRollApp/Sources/Models/Event.swift",
      "find": "case combo     = \"Combo\"",
      "replace": "case combo     = \"Comboo\"",
      "test": "tests/test_event_manifest.py::test_the_shoot_type_table_matches_the_swift_it_mirrors",
      "breaks": "the manifest tool's shoot type table drifts from the Swift enum"
    },
    {
      "name": "design-fingerprint-moves",
      "file": "postroll/media/program_plate.py",
      "find": "PRINT_Y = 430",
      "replace": "PRINT_Y = 431",
      "test": "tests/test_media_design_fingerprint.py::test_the_template_still_renders_the_design_its_version_claims[reel_morph]",
      "breaks": "a plate geometry change ships without a design version bump"
    },
    {
      "name": "dead-tuning-control",
      "file": "postroll/media/generate_reel_morph.py",
      "find": "FPS = 30",
      "replace": "RAW_DESATURATION = 0.0\nFPS = 30",
      "test": "tests/test_no_dead_tuning_controls.py::test_the_reel_generator_declares_nothing_it_never_reads[generate_reel_morph]",
      "breaks": "a tuning constant nothing reads lands in a reel generator (L46)"
    },
    {
      "name": "wordmark-required-cover",
      "file": "postroll/ai/generate_cover.py",
      "find": "logo_path=required_wordmark(",
      "replace": "logo_path=str(",
      "test": "tests/test_wordmark_required.py::test_no_app_call_site_turns_a_missing_wordmark_into_no_mark[generate_cover]",
      "breaks": "a missing wordmark file becomes a cover with no mark instead of an error"
    },
    {
      "name": "screen-reel-dark-mark",
      "file": "postroll/ai/generate_media.py",
      "find": "# by a measured pixel test.\n                            logo_path=required_wordmark(LOGO_BLACK),",
      "replace": "# by a measured pixel test.\n                            logo_path=required_wordmark(LOGO_WHITE),",
      "test": "tests/test_screen_reel_logo_contrast.py::test_the_screen_reel_is_given_the_dark_mark",
      "breaks": "the screen reel gets the white mark on a light surface, invisible again"
    },
    {
      "name": "hashtag-gate-prompt",
      "file": "postroll/ai/generate_captions.py",
      "find": "org/show, composer/playwright/band, genre.",
      "replace": "org/show, composer/playwright/band, performers visible, genre.",
      "test": "tests/test_performer_hashtag_gate.py::test_the_prompt_no_longer_demands_performer_hashtags",
      "breaks": "the caption prompt contradicts the performer hashtag fame gate"
    },
    {
      "name": "prd-status-honest",
      "file": "postroll-prd.md",
      "find": "uploads that to Metricool by hand.",
      "replace": "uploads that to the scheduler by machine.",
      "test": "tests/test_prd_matches_the_build.py::test_the_status_says_what_actually_happens_today",
      "breaks": "the PRD claims publishing the code does not do (L32)"
    },
    {
      "name": "docs-ai-cost-honest",
      "file": "postroll-prd.md",
      "find": "- Metricool: $216/year (unchanged)",
      "replace": "- Metricool: $216/year (unchanged), Claude Code: $0",
      "test": "tests/test_paid_default_path.py::test_the_docs_do_not_claim_the_ai_is_free",
      "breaks": "the docs claim the AI path is free when the default path is metered"
    },
    {
      "name": "appkit-wrapper-lifetime",
      "file": "PostRollApp/Sources/Views/MainWindowView.swift",
      "find": "/// Lifetime, audited against #196 (#198): this hands nothing to anything that",
      "replace": "/// This wrapper hands nothing to anything that",
      "test": "PostRollTests/AppKitWrapperLifetimeTests/testEveryAppKitWrapperHasAnsweredTheLifetimeQuestion",
      "breaks": "an AppKit wrapper loses its recorded answer to the lifetime question (#196)"
    },
    {
      "name": "blog-heading-by-hand",
      "file": "PostRollApp/Sources/Services/EventExporter.swift",
      "find": "let md = BlogDraftText.copyText(title: blog.title, body: blog.body) + \"\\n\"",
      "replace": "let md = \"# \\(blog.title)\\n\\n\\(blog.body)\" + \"\\n\"",
      "test": "PostRollTests/BlogDraftTextContractTests/testNoScreenBuildsTheHeadingByHand",
      "breaks": "a screen assembles the blog heading by hand instead of the shared renderer"
    },
    {
      "name": "collaborator-sort-default",
      "file": "PostRollApp/Sources/Services/CollaboratorPick.swift",
      "find": "let followers = candidate.stats?.followers, followers > 0",
      "replace": "let followers = Optional(candidate.stats?.followers ?? 0), followers > 0",
      "test": "PostRollTests/CollaboratorPickTests/testTheSortCannotDefaultAMissingFigureToZero",
      "breaks": "a missing follower figure silently scores as zero (L50)"
    },
    {
      "name": "review-staleness-refresh",
      "file": "PostRollApp/Sources/Views/CaptionReviewView.swift",
      "find": ".onAppear { refreshDesignStaleness() }",
      "replace": ".onAppear { }",
      "test": "PostRollTests/LayoutSidecarContentsTests/testTheReviewScreenReadsTheStampOnceRatherThanEveryRedraw",
      "breaks": "the review screen never refreshes design staleness on appear"
    },
    {
      "name": "discarded-write-allowlist",
      "file": "PostRollApp/Sources/Services/AppPaths.swift",
      "find": "try? fm.copyItem(at: seed, to: dest)",
      "replace": "try? fm.copyItem(at: seed, to: dest as URL)",
      "test": "PostRollTests/DiscardedFileWriteGuardTests/testNoFileProducingWriteHasItsFailureThrownAway",
      "breaks": "a discarded file-write stops matching its allowlist entry (#362)"
    },
    {
      "name": "sentence-rule-copy",
      "file": "PostRollApp/Sources/Services/StoreSaveGate.swift",
      "find": "let reason = Sentence.closed(error.localizedDescription)",
      "replace": "let raw = error.localizedDescription; let reason = \".?!\".contains(raw.last ?? \" \") ? raw : raw + \".\"",
      "test": "PostRollTests/InterpolatedReasonTests/testNoScreenKeepsItsOwnCopyOfTheRule",
      "breaks": "a store grows its own copy of the sentence-closing rule, which is where the unreadable-store message is now built (#439)"
    },
    {
      "name": "manifest-day-rule-copy",
      "file": "PostRollApp/Sources/Services/PythonBridge.swift",
      "find": "var dayEntry = ManifestDay.sharedEntry(pd, day: dayName)",
      "replace": "guard !pd.photoPaths.isEmpty || pd.rawPhotoPath != nil || !pd.clipPaths.isEmpty else { continue }; var dayEntry = ManifestDay.sharedEntry(pd, day: dayName)",
      "test": "PostRollTests/ManifestDaySharedEntryTests/testNeitherBuilderCarriesItsOwnCopyOfTheRule",
      "breaks": "a manifest builder grows its own copy of the day-inclusion rule"
    },
    {
      "name": "picker-storage-route",
      "file": "PostRollApp/Sources/Views/CaptionReviewView.swift",
      "find": "let outcome = ImportedPicks.copy(panel.urls)",
      "replace": "let outcome = ImportedPicks.Outcome(stored: panel.urls, failures: [])",
      "test": "PostRollTests/PickerStorageHygieneTests/testEveryPickerRoutesItsPickIntoAppStorage",
      "breaks": "a picker stores raw picked URLs without copying them in (the TCC bug class)"
    },
    {
      "name": "test-seam-stays-out",
      "file": "PostRollApp/Sources/AppState.swift",
      "find": "#if POSTROLL_TESTS",
      "replace": "#if true",
      "test": "PostRollTests/TestTargetHygieneTests/testTheAppStateTestSeamStaysOutOfTheShippingApp",
      "breaks": "the test-only seam ships in the app target"
    },
    {
      "name": "paid-action-ink",
      "file": "PostRollApp/Sources/Views/CaptionReviewBodies.swift",
      "find": "                        .foregroundStyle(Color.roseGold)",
      "replace": "                        .foregroundStyle(Color.warmMid)",
      "test": "PostRollTests/VisibleControlGuardTests/testNoPaidActionIsDrawnInTheBodyTextColour",
      "breaks": "a paid action is drawn in the body text colour and stops reading as a control (L49)"
    },
    {
      "name": "recurring-banner-control",
      "file": "PostRollApp/Sources/Views/ExportView.swift",
      "find": "RecurringAccounts.actionable(recurringAccounts)",
      "replace": "Array(recurringAccounts.prefix(3))",
      "test": "PostRollTests/RecurringAccountsTests/testTheBannerOffersAControlForEachAccountItNames",
      "breaks": "the recurring-accounts banner names accounts it offers no control for (L80)"
    },
    {
      "name": "macos-pr-cancellation-scoped",
      "file": ".github/workflows/swift.yml",
      "find": "cancel-in-progress: ${{ github.event_name == 'pull_request' }}",
      "replace": "cancel-in-progress: true",
      "test": "tests/test_ci_caches_and_cancels.py::test_a_push_to_main_is_never_cancelled[macos]",
      "breaks": "cancellation stops being scoped to pull requests, so a merge to main is cancelled by the next one and main is left unverified (#430)"
    },
    {
      "name": "linux-pr-cancellation-scoped",
      "file": ".github/workflows/tests.yml",
      "find": "cancel-in-progress: ${{ github.event_name == 'pull_request' }}",
      "replace": "cancel-in-progress: true",
      "test": "tests/test_ci_caches_and_cancels.py::test_a_push_to_main_is_never_cancelled[linux]",
      "breaks": "same, on the Linux workflow (#430)"
    },
    {
      "name": "macos-concurrency-group-per-commit",
      "file": ".github/workflows/swift.yml",
      "find": "group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}",
      "replace": "group: ${{ github.workflow }}-${{ github.ref }}",
      "test": "tests/test_ci_caches_and_cancels.py::test_consecutive_merges_do_not_queue_behind_one_another[macos]",
      "breaks": "the group goes back to the ref, so back-to-back merges queue one behind the other (#430)"
    },
    {
      "name": "derived-data-actually-built-into",
      "file": ".github/workflows/swift.yml",
      "find": "            -derivedDataPath PostRollApp/.derived-data \\\n",
      "replace": "",
      "test": "tests/test_ci_caches_and_cancels.py::test_the_swift_tests_build_into_the_folder_that_is_cached",
      "breaks": "the cache is restored and saved while xcodebuild builds somewhere else, so it is uploaded and never read (L3)"
    },
    {
      "name": "derived-data-cache-capped",
      "file": ".github/workflows/swift.yml",
      "find": " && steps.cache-size.outputs.over-cap == 'false' }}",
      "replace": " }}",
      "test": "tests/test_ci_caches_and_cancels.py::test_the_build_cache_is_measured_against_a_cap_before_it_is_uploaded",
      "breaks": "the folder is uploaded however large it is, evicting the other caches (#430, overture#2585)"
    },
    {
      "name": "pip-wheels-cached",
      "file": ".github/workflows/tests.yml",
      "find": "          cache: pip",
      "replace": "          # cache: pip",
      "test": "tests/test_ci_caches_and_cancels.py::test_the_python_workflow_caches_its_wheels",
      "breaks": "every run re-downloads the same pinned wheels, and a comment still names the setting (L103)"
    },
    {
      "name": "ffmpeg-cache-fallback",
      "file": ".github/workflows/tests.yml",
      "find": "              install_from_apt\n            }",
      "replace": "              echo \"carrying on without it\"\n            }",
      "test": "tests/test_ci_caches_and_cancels.py::test_a_cold_or_refused_package_cache_still_installs_ffmpeg",
      "breaks": "a cached package set dpkg refuses leaves the job with no ffmpeg at all (#430)"
    },
    {
      "name": "full-local-run-includes-the-slow-files",
      "file": "Makefile",
      "find": "-q -n auto",
      "replace": "-q -n auto -m \"not slow\"",
      "test": "tests/test_fast_subset_stays_honest.py::test_the_full_target_still_runs_the_slow_files",
      "breaks": "the full local run stops running the four files that render real reels (#430, #497)"
    },
    {
      "name": "full-local-run-includes-the-fast-tests",
      "file": "Makefile",
      "find": "tests/ -q -n auto",
      "replace": "tests/ -q -n auto -m slow",
      "test": "tests/test_fast_subset_stays_honest.py::test_the_full_target_still_runs_the_ordinary_tests_too",
      "breaks": "the full local run drops the 1700 ordinary tests and runs only the reels (#430, #497)"
    },
    {
      "name": "full-local-run-stays-parallel",
      "file": "Makefile",
      "find": "pytest tests/ -q -n auto",
      "replace": "pytest tests/ -q",
      "test": "tests/test_reference_frames_are_safe_to_parallelise.py::test_the_whole_suite_is_run_in_parallel",
      "breaks": "the full local run goes back to serial, six minutes slower, with nothing saying so (#497)"
    },
    {
      "name": "install-gate-uses-one-spelling",
      "file": "PostRollApp/build-install.sh",
      "find": "    make -C \"${REPO_ROOT}\" test-python",
      "replace": "    (cd \"${REPO_ROOT}\" && venv/bin/python -m pytest tests/ -q)",
      "test": "tests/test_build_and_test_gates.py::test_the_install_gate_runs_the_suite_the_make_target_defines",
      "breaks": "the install gate keeps its own copy of the pytest command, which then drifts from the Makefile's (#430)"
    },
    {
      "name": "source-tree-guard-armed",
      "file": "tests/conftest.py",
      "find": "@pytest.fixture(autouse=True, scope=\"module\")",
      "replace": "@pytest.fixture(scope=\"module\")",
      "test": "tests/test_reference_frames_are_safe_to_parallelise.py::test_the_source_tree_guard_is_still_armed",
      "breaks": "the guard stops applying to every module, so the next test that rewrites a real source file is found as a flaky parallel run (#497)"
    },
    {
      "name": "no-paths-filter-narrows-the-macos-checks",
      "file": ".github/workflows/swift.yml",
      "find": "  pull_request:\n    branches: [main]\n  workflow_dispatch:",
      "replace": "  pull_request:\n    branches: [main]\n    paths:\n      - \"PostRollApp/**\"\n  workflow_dispatch:",
      "test": "tests/test_ci_gates.py::test_no_paths_filter_narrows_what_a_pull_request_runs",
      "breaks": "a paths filter comes back, so some pull requests skip the macOS checks and a skipped check reads as a passing one (#431)"
    },
    {
      "name": "macos-halves-stay-separate-jobs",
      "file": ".github/workflows/swift.yml",
      "find": "  reference-frames:\n    runs-on: macos-15\n    steps:\n",
      "replace": "",
      "test": "tests/test_ci_gates.py::test_the_two_halves_run_as_separate_jobs",
      "breaks": "the reference frames go back into the Swift job, so the quick compile signal waits on seven minutes of rendering reels (#431)"
    },
    {
      "name": "install-gate-fast-subset",
      "file": "PostRollApp/build-install.sh",
      "find": "make -C \"${REPO_ROOT}\" test-python-fast",
      "replace": "make -C \"${REPO_ROOT}\" test-python-nonexistent",
      "test": "tests/test_build_and_test_gates.py::test_the_install_gate_runs_the_fast_subset_and_ci_still_runs_everything",
      "breaks": "the install gate stops running the fast subset, so an install is gated on nothing (#432)"
    },
    {
      "name": "analytics-save-gate",
      "file": "PostRollApp/Sources/Services/AnalyticsStore.swift",
      "find": "guard !StoreSaveGate.shared.isBlocked(fileURL) else {",
      "replace": "guard !StoreSaveGate.shared.isBlocked(URL(fileURLWithPath: \"/nowhere\")) else {",
      "test": "PostRollTests/AnalyticsStoreRecoveryTests/testSavingIsRefusedWhileTheFileCouldNotBeRead",
      "breaks": "analytics.json is written over even though the last load could not read it (#439)"
    },
    {
      "name": "analytics-file-exists-precheck",
      "file": "PostRollApp/Sources/Services/AnalyticsStore.swift",
      "find": "        let data: Data\n        do {\n            data = try Data(contentsOf: url)",
      "replace": "        guard FileManager.default.fileExists(atPath: url.path) else { return }\n        let data: Data\n        do {\n            data = try Data(contentsOf: url)",
      "test": "PostRollTests/AnalyticsStoreRecoveryTests/testADeniedFolderIsNotMistakenForAFreshInstall",
      "breaks": "the fileExists precheck returns, so a TCC-denied file reads as a fresh install (#439)"
    },
    {
      "name": "not-found-rule-one-copy",
      "file": "PostRollApp/Sources/Services/DataMigration.swift",
      "find": "        (error as NSError).isFileNotFound\n    }",
      "replace": "        (error as NSError).code == NSFileReadNoSuchFileError\n    }",
      "test": "PostRollTests/StoreSaveGateTests/testNoStoreKeepsItsOwnCopyOfTheNotFoundRule",
      "breaks": "a store grows its own copy of the absent-versus-unreadable rule again, and this one misses the POSIX spelling (#439)"
    },
    {
      "name": "legacy-store-absent-both-spellings",
      "file": "PostRollApp/Sources/Services/DataMigration.swift",
      "find": "        (error as NSError).isFileNotFound\n    }",
      "replace": "        (error as NSError).code == NSFileReadNoSuchFileError\n    }",
      "test": "PostRollTests/DataMigrationTests/testAnAbsentLegacyStoreIsRecognisedInEitherSpelling",
      "breaks": "only the Cocoa spelling of not-found counts as a fresh install, so a POSIX ENOENT read leaves the migration marker unwritten and retrying on every launch (#439)"
    },
    {
      "name": "import-notice-not-a-success",
      "file": "PostRollApp/Sources/Services/InsightsDisplay.swift",
      "find": "            return .notSaved(counts + \" into this window, but nothing was saved: your \"",
      "replace": "            return .saved(counts + \" into this window, but nothing was saved: your \"",
      "test": "PostRollTests/ImportNoticeTests/testARefusedSaveIsNotShownAsASuccess",
      "breaks": "an import the store refused to write is drawn beside a green tick, so Dan reads it as kept and finds it gone at next launch (#439, L12)"
    },
    {
      "name": "save-failure-surfaced",
      "file": "PostRollApp/Sources/AppState.swift",
      "find": "            saveFailure = SaveFailureNotice.message(reason: reason)",
      "replace": "            NSLog(\"save failed: \\(reason)\")",
      "test": "PostRollTests/AppStateSaveFailureTests/testAFailedSaveIsSurfacedRatherThanLogged",
      "breaks": "a save that starts failing mid-session goes back to reaching NSLog and nothing else, so an evening of captions is lost by quitting (#446)"
    },
    {
      "name": "save-failure-banner-offers-retry",
      "file": "PostRollApp/Sources/Views/MainWindowView.swift",
      "find": "                    actions: [BrandBannerAction(label: SaveFailureNotice.retryLabel) {\n                        appState.retrySave()\n                    }]",
      "replace": "                    actions: []",
      "test": "PostRollTests/SaveFailureBannerTests/testItOffersTheRetryThatCanClearIt",
      "breaks": "the failure banner loses the only control that can clear it, leaving a message that names a problem with nowhere to go (L80)"
    }
  ],
  "comment_uncovered": "Guards deliberately NOT registered, because no compile-safe one line perturbation expresses the defect they catch: UploadPageCropRemovalTests (adding a real crop control is not one line; prose would trip it but proves nothing), the absence guards test_blog_draft_text.py::test_no_module_builds_the_heading_by_hand / test_layout_sidecar.py::test_no_generator_builds_the_name_by_hand / test_blog_meta.py::test_neither_string_can_reach_the_ai_round_trip (their only one line trip is planting the banned text artificially), test_suite_hygiene.py and test_guard_mutation_registry.py (they protect the test suite and this registry themselves), test_bridge_payload_contract.py / test_manifest_contract.py (the one line rename anchor is ambiguous in PythonBridge.swift), test_brand_text.py (removing the route in one line breaks the module's own names), test_event_slug_parity.py (slug internals are not safely one line perturbable), and the measured pixel thresholds in test_golden_frames.py / test_frame_legibility.py / test_screen_reel_logo_contrast.py's contrast checks (failing them takes a rendering change, not a text edit)."
}
