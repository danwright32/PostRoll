# Pinned exactly, not floored (#105). A ">=" line means every CI run installs
# whatever shipped that morning, so an upstream release breaks the build with no
# change on our side, which has already happened once. Bump these deliberately.
Pillow==11.3.0
anthropic==0.96.0

# Test-only, pinned for the same reason: CI installed pytest unpinned.
pytest==8.4.2
# Runs the reference-frame checks in parallel. They are 9.8 of the macOS job's
# 13 minutes, and they are safe to parallelise because every one of them writes
# only into pytest's per-test tmp_path (#412).
pytest-xdist==3.8.0
# Gives each test its own deadline, so a wedged one is a red line naming itself
# rather than a job killed by `timeout-minutes` saying only that the job ran out
# of time (#836). Configured in pyproject.toml's addopts; without this pin every
# pytest invocation would die on an unrecognised `--timeout`.
pytest-timeout==2.4.0

# Static checks, run in CI before the suite (#113).
ruff==0.16.2

# Deliberately NOT here: mypy (#243).
#
# Measured rather than assumed, on 2026-08-10. Run against the five newest and
# best annotated modules (ai/transport.py, ai/claude_client.py, ai/progress.py,
# ai/caption_quality.py, media/probe.py) it reports four findings, all in
# claude_client, and none of them is a defect:
#
#   * two say the token counts passed to the usage log might be None, which the
#     isinstance guard three lines above already rules out and mypy cannot
#     narrow through;
#   * one says Image.LANCZOS is not an attribute, which it is at the Pillow
#     version pinned above;
#   * one objects to the shape of a message content list the SDK accepts.
#
# So turning it on today would mean shipping four suppressions and no fixes,
# which is a check that passes by being told to. The pipeline is guarded by
# 1200+ tests and ruff instead. Revisit if the annotations spread far enough
# that mypy has something to reason about, and start from these numbers rather
# than from scratch.
